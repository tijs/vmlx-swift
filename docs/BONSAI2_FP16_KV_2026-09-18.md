# Bonsai2 attention KV precision

Status: focused component and exact-pinned native-app acceptance completed.
No matched speedup claim. Osaurus final squash-commit repin/CI is tracked in PR2820.
Base:125f961272908697f87a983f972ccf50e0810a05 (merged packed-load/media-prefill).

## Mechanism and scope

Bonsai2 preserves its exact F16 affine metadata and F32 norms, signs and state
projections. MLX rms_norm uses result_type(input,weight), so F32 norm weights
promote the activation path. Attention currently caches projected K/V without
an explicit storage dtype. The retained native app logs show F32 GDN activations;
the configured FP16 cache label alone does not establish attention tensor dtype.

Implement FP16 Q/K/V only at the attention boundary after normalization/RoPE,
selected by actual Hadamard projection modules, for both text and VLM routes.
Preserve the projection/gating output dtype and all stored weights. GDN/SSM
recurrence remains F32: rounding recurrent state at cache boundaries changes the
recurrence and is not equivalent to reducing attention KV storage.

The same policy must apply on cold, prefix-restored, rotating and batched paths.
Keep cache offset/ring metadata intact when converting a pre-existing floating
attention cache. Quantized/TurboQuant storage must not be reinterpreted as plain
K/V. ModelContainer cache keys must distinguish the new numerical policy from
old F32 checkpoints. Media, reasoning and tool/schema salts remain unchanged.

## Restore regression caught before app merge

The first executable component run at 1623bb45 built successfully, then reported
32 issues across 47 tests: 26 exposed the existing standard disk restore's
unconditional FP16-to-BF16 conversion; 6 were new tests reading the compiled
ring's intentionally stale Swift offset instead of its live tensor counter.
Cold/decode precision, batched attention, both tiny VLM towers, actual-bundle
protocol/media processing and the remaining suites passed. These are component
results, not full-model native-app proof.

The correction makes dtype preservation model-owned and opt-in. Both container
caching entrypoints select it from installed Hadamard modules. A typed disk
marker preserves native standard K/V on reopen; paged restore carries the same
coordinator policy through AR, batch and speculative restore call sites. Other
models, unmarked old records, QSA and legacy disk formats keep their previous
behavior. GDN/SSM state serialization and recurrence are unchanged. Malformed
precision metadata is a clean cache miss. Strict tensor/logit equality for a
canonical tool-boundary disk reopen is retained, not relaxed.

Actual old native run8 files at 10,099 tokens contained 32 F32 attention tensors
(1,323,696,128 bytes) and 48 F32 recurrent tensors (150,994,944 bytes), despite the
UI's nominal FP16 label. Their headers, exact logged hashes and file sizes are
retained in `fp16-baseline-run8-cache-dtypes.json` under the evidence directory.

## Bounded acceptance

- [x] Actual baseline and candidate K/V dtypes, bytes, output error and finite
  values; no nominal-mode-only claim.
- [x] Both text/VLM Hadamard routes; non-Hadamard behavior unchanged; stored
  signs/scales/norms and GDN recurrent state unchanged.
- [x] Plain, rotating, batched and compiled-cache metadata/storage regression;
  real disk reopen after a canonical tool boundary, then suffix/decode parity.
- [x] Real image and per-tool app continuations for both local Bonsai2 storages,
  sequentially, native generation defaults, one loaded at a time. Record prefill,
  cache-hit counts, tok/s, physical footprint, visible output and any failures.
- [ ] Separate engine PR, scoped review/tests/evidence comment, merge; then exact
  Osaurus pin PR and ordinary CI/merge. No release/tag/install/dispatch.

Private raw evidence: /Users/eric/vmlx-private-evidence/runtime-followup-2026-09-18.
The earlier native runs are context, not proof of this new precision change.

## Current source and executable evidence

SOURCE EVIDENCE: engine73ebf52507743a871bac58c432e9c0522bf262c2;
`JangHadamardAttention.swift`, the LLM/VLM `Qwen35` attention call sites,
`ModelContainer`, `CacheCoordinatorConfig`, `CacheHelpers`, `TQDiskSerializer`
and the five paged-restore callers. Tests are `Bonsai2AttentionPrecisionTests`
and `Qwen35HadamardCacheTests`. This final documentation update changes no
production or test source from the tested revision.

LIVE EVIDENCE: `SWIFTTEST_BonsaiFP16Components__090002.log` and
`fp16-73ebf52507743a871bac58c432e9c0522bf262c2-four-swift-testing.xml`:
49 tests /9 suites passed in45.535s. Actual disk reopen preserves F16/BF16/F32
only under the typed opt-in; unmarked legacy restores retain their prior
behavior. Strict canonical tool-boundary tensor/logit equality was not relaxed.

Optimized native development appf21ea6e21c640f8c032273ccc01c9cb17cdf94bb pins
that exact engine. BinarySHA256:
`adff63f38576441a1d010797a12446d0cff55bffa592709386f73f73086ee52a`;
UUID`6863D2B0-7A59-3892-9E03-7FB54E2F75C9`. Build receipt:
`fp16-pin-build-f21ea6e21c640f8c032273ccc01c9cb17cdf94bb-one.log`.
Native UI/file-picker actions, visual inspection, and read-only runtime/SQLite
captures were used. These were not API-only prompts.

On authorized M5 Max2, sequential1.75-bit and ternary sessions each completed
three user turns: real-image identification, file_write then file_read with a
correct final answer, then correct no-tool recall. All10 generation steps had
natural stop/tool completion; no loop, protocol leak, reasoning-only result,
length stop or app crash was observed in this bounded workload.

| Storage | Prompt tokens per step | Prompt ms per step | Generated tok/s per step | Disk-restored tokens |
| --- | --- | --- | --- | --- |
| 1.75-bit | 2943,3141,3498,3693,3865 | 21874,8778,1265,947,797 | 16.6,16.9,18.0,16.5,16.9 | 3134,3491,3686 |
| Ternary | 2943,3135,3519,3715,3891 | 8288,8762,1893,964,659 | 18.5,16.4,16.4,16.3,17.5 | 3128,3512,3708 |

Short outputs are14-85tokens; this is not a sustained decode benchmark or
matched pre/post speed comparison. Prompt-throughput counters include reused
tokens; do not interpret them as uncached prefill throughput. The required-tool
second user turn does fresh prefill; subsequent tool continuations restore all
64layers. Five disk stores and three full disk restores per three-turn session;
store/drain completion precedes the next tool execution/continuation. Prefix
and paged RAM hits remain0; do not claim TurboQuant or paged KV activation.

All nine inspected new checkpoint files contain32F16 attention K/V tensors,
48F32 recurrent tensors and48F32 convolution tensors; preservation marker1.
At2936tokens each storage has192,413,696bytes of attention K/V and unchanged
150,994,944bytes of recurrence. At the attention boundary the live trace reports
F16 output/storage; the GDN trace remainsF32. Weights, norms and samplers were
not narrowed or masked. Effective submissions: temperature1,topP.95,topK20,
minP0,MTPoff; UI reasoningNone. Actual media salt and tool schemas retained.

Raw receipts under the private evidence directory: `fp16p2.stdout`,
`fp16t.stdout`, both `*-prefill-full.log` (offsets in `*-launch.json`),
`live-captures/fp16p2-final`, `live-captures/fp16t-final`,
`fp16p2-vision-cache-dtypes.json`, `fp16p2-tools-recall-cache-dtypes.json`,
`fp16t-cache-dtypes.json`, and `fp16{p2,t}-{vision,tools,recall}-done.png`.
The image was redcircle/bluesquare/greentriangle left-to-right, with BIRCH below.
Both tool-created files and all visible answers matched it.

Packed guard093446: exit0/cleanup0, peak tracked physical footprint11.02GiB,
swap3.60->3.50GiB. Ternary guard094308: exit0/cleanup0,peak9.37GiB,
swap3.50->3.49GiB. Host pressure remained normal. A first proof-launch attempt
aborted on host free-memory threshold before any model prompt; retained in
`MERGE-CLOSEOUT.md`, not counted as a pass. A UI selection retry in fp16t first
ran one extra packed image turn (correct,17.0tok/s,2936token disk restore), then
unloaded it before selecting ternary. It is not counted as ternary evidence.
No concurrent model load, model-file edits, release, tag or installation.

CI caveat: engine run35365781761 has an advisory formatter failure, including
wrapping/indentation of new argument lines as well as extensive existing diffs;
it is not all-green or entirely pre-existing. Four Linux builds passed, Mac/CUDA
jobs were pending at native proof closeout. The scoped local Metal regressions
and native evidence above are the merge evidence, not a claim those pending
jobs passed. Osaurus candidate CI35365974786 passed all four required gates
(test-core,test-cli,swiftlint,shellcheck). Final repin must pass its own checks.
No16GBM4, universal-model, long-context, audio/video or sustained-speed claim.
